version: 1.0
runtime: python311
build:
  commands:
    build:
      - cd backend  # 👈 This tells App Runner to go into your backend folder first
      - pip install -r requirements.txt
run:
  command: gunicorn --bind 0.0.0.0:8080 app:app
  network:
    port: 8080